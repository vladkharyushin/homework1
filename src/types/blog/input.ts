export type BlogParams = {
    id: string
    name: string;
    description: string;
    websiteUrl: string;
};

export type UpdateBlogData = {
    name: string;
    description: string;
    websiteUrl: string;
};

export type InputBlogType = {
    name: string;
    description: string;
    websiteUrl: string;
};