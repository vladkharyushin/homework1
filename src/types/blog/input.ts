export type BlogParams = {
    id: string
    name: string;
    description: string;
    websiteUrl: string;
};

export type UpdateBlogData = {
    id: string
    name: string;
    description: string;
    websiteUrl: string;
};

export type InputBlogType = {
    id: string
    name: string;
    description: string;
    websiteUrl: string;
};